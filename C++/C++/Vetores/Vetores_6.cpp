#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <conio.h>
main(void)
{int i,cod[15];
 float nota[15];
	for (i=0;i<15;i++)
	{
		printf("Digite o codigo do aluno %i: ",i+1);
		scanf("%i",&cod[i]);
		printf("Digite a nota do aluno %i: ",i+1);
		scanf("%f",&nota[i]);
		if ()
	}
	media=total/i;
	printf("\n\nValores maiores que a media:\n");
	for (i=0;i<15;i++)
	{
		if (val[i]>media) printf("%i\n",val[i]);
	}
}
